{
  "name": "noema-live-games-tiktok",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive TikTok LIVE gaming platform powered by NOEMA",
  "license": "MIT",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @noema/live-game --filter @noema/game-gateway dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  }
}
